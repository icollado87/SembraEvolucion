Status pill. `success` = Aprobada/Pagada/Aplicada; `warning` = Vencida/Pendiente; `danger` = Cancelada/Rechazada; `neutral` for informational states.

```jsx
<Badge tone="success">Aprobada</Badge>
```